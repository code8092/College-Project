spring.application.name=RoomKhojo

# ? Use Docker service name "mysql", not localhost
spring.datasource.url=jdbc:mysql://localhost:3306/YaariStay
spring.datasource.username=root
spring.datasource.password=rajAnand@123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Optional upload directory (relative path is safer in Docker)
file.upload-dir=/opt/RoomKhojo/uploads/

# ? Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.display-request-duration=true

admin.email=admin@roomkhojo.com
admin.password=admin123
